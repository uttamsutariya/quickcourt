// This component is now just a re-export of AppLayout for backward compatibility
// The sidebar has been removed in favor of a consistent navbar layout
import AppLayout from "@/components/shared/AppLayout";

const OwnerLayout = AppLayout;

export default OwnerLayout;
